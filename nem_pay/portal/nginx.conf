server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  # SPA fallback: client-side routes (e.g. /balances, /payments/:id) resolve to index.html so a
  # deep link or refresh does not 404.
  location / {
    try_files $uri /index.html;
  }
}
